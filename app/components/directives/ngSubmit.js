(function(module){

    var ngSubmit = function() {
        return {
            link: function (scope, element, attrs) {

            }
        };
    }
    module.directive('ngSubmit', ngSubmit);

}(angular.module('app')));