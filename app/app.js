(function () {
    "use strict";
    // Each component will define it's own translations as a property of the
    // `i18nTranslations` global object. This needs to be defined here before any
    // module.
    angular.module("app", ["ngRoute", "ui.bootstrap"]);

    angular.module("app").run(function () {
    });

    angular.module("app").config(function () {
    });
})();
